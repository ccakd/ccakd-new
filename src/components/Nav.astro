---
import type { Locale } from '../i18n/utils';

interface Props {
  locale: Locale;
  currentPath: string;
}

const { locale, currentPath } = Astro.props;
---

<!-- Stub: replaced in Task 6 -->
<header></header>
